rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Fully permissive read/write for development sandbox to prevent any permission issues
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
